// Guess the output here

class Task {
    // let test = 2
    constructor(id) {
        this.id = id
    }
}

Task.osName = "windows";
let task = new Task();

console.log(task.osName) // ?? what is OP