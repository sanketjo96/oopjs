// Guess the output here

class Task {
    // let test = 2
    constructor(id) {
        this.id = id
    }
}

Task.osName = "windows";
let task = new Task(134);

console.log(Task.osName)
console.log(task.osName) // ?? what is OP